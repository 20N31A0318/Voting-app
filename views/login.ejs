<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" contents="<%=csrfToken%>">
    <link rel="stylesheet" href="./css/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>
        <%=title%>
    </title>
</head>

<body>
    <div class="flex justify-center">
        <div class="w-fit">
            <%- include('header.ejs')%>
                <h6 class="py-4 text-lg">Login into your account</h6>
                <form action="/session" method="post">
                    <input type="hidden" name="_csrf" value="<%=csrfToken%>" >
                    <div class="py-3 p-2 hover:bg-sky-100 rounded">
                        <input type="email" name="email" placeholder="Email" class="border border-blue-600 rounded p-2">
                    </div>
                    <div class="py-3 p-2 hover:bg-sky-100 rounded">
                        <input type="password" name="password" placeholder="Password"
                            class="border border-blue-600 rounded p-2">
                    </div>
                    <div class="py-3">
                        <button type="submit"
                            class="bg-blue-600 hover:bg-blue-800 text-white px-5 py-1.5 rounded font-medium mr-2 mb-2">Login</button>
                    </div>
                </form>
                <% if(messages && messages.error && messages.error.length > 0)  { %>
                    <% for(var i=0; i<messages.error.length; i++) { %>
                            <li> <%= messages.error[i] %></li>
                    <% } %>
                  <% } %>
        </div>
    </div>
</body>

</html>