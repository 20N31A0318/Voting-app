<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" contents="<%=csrfToken%>">
    <link rel="stylesheet" href="/css/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>
        <%=title%>
    </title>
</head>
<script>
    function addQuestion() {

    }

    function submitForm() {
        document.getElementById("main-form").submit()
    }
</script>

<body>
    <div class="flex justify-center">
        <div class="w-[500px]">
            <%- include('header.ejs') %>
                <h3 class="text-2xl text-blue-600 font-bold my-4 block ">Questions :</h3>
                <form id="main-form" action='/Election/<%=electionId%>/addQuestion' method="post" class="mt-2"></form>

                <input type="hidden" name="_csrf" value="<%=csrfToken%>" form="main-form">

                <div class="flex-auto pb-2">
                    <input type="text" name="question" placeholder="Add a question"
                        class="border border-blue-600 rounded text-gray-900 w-full p-2 text-sm" form="main-form">
                </div>

                <div class="flex-auto">
                    <textarea name="description" id="description" cols="30" rows="5"
                        placeholder="Description of the question"
                        class="border border-blue-600 rounded text-gray-900 w-full p-2 text-sm leading-10" form="main-form"></textarea>
                </div>

                <div class="flex-none mt-2">
                    <button
                        class="text-lg text-center block font-bold ease-in-out duration-300 text-white rounded py-2 px-2 bg-blue-600 hover:bg-blue-800 w-full" onclick="submitForm()">Submit</button>
                </div>
        </div>
</body>

</html>